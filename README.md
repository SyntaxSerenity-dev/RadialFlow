# RadialFlow
Lightweight JS library for radial circular menus. Features: 4 menu types (Classic, Labeled, Modern, Corner), infinite submenus, auto-pagination, dimming effects, customizable callbacks, smooth animations, and zero dependencies. Easy to integrate!
